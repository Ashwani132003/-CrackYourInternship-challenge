

class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        
        
        d={}
        for item,value in enumerate(nums):
            if value not in d:
                d[value] = 1
            else:
                return value
